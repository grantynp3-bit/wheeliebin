<?xml version="1.0" encoding="utf-8"?>
<!-- Wraps the logo bitmap with an inset so it sits inside the adaptive icon's safe
     zone (the visible circle is ~66/108 of the canvas) and never gets clipped by
     the launcher's mask shape, whatever that shape is. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/image"
    android:insetLeft="19%"
    android:insetRight="19%"
    android:insetTop="19%"
    android:insetBottom="19%" />
